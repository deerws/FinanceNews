[project]
name = "financenews"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "httpx>=0.28.1",
    "lxml>=6.1.1",
    "pdfplumber>=0.11.10",
    "pyyaml>=6.0.3",
    "typer>=0.27.0",
]

[project.scripts]
financenews = "financenews:main"

[build-system]
requires = ["uv_build>=0.11.9,<0.12.0"]
build-backend = "uv_build"
