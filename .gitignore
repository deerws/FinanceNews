# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Dados baixados: PDFs de terceiros (direitos autorais) + índice gerado
# (barra na frente: só a pasta na raiz, não web/app/(app)/cartas/ do Next.js)
/cartas/

# Estado local do Claude Code (não é conteúdo do projeto)
.claude/

# Segredos (service_role key do Supabase etc.) — nunca commitar
.env
.env.local

